<Window x:Class="MeshhessenClient.DirectMessagesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Direktnachrichten"
        Height="500"
        Width="700"
        MinHeight="300"
        MinWidth="500">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tabs für verschiedene DM-Partner -->
        <TabControl x:Name="DmTabControl" Grid.Row="0">
            <!-- Tabs werden dynamisch hinzugefügt -->
        </TabControl>

        <!-- Status Bar -->
        <Border Grid.Row="1" Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}" Padding="10,5">
            <TextBlock x:Name="StatusText" Text="Keine aktiven Chats" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
